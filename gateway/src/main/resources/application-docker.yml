server:
  port: 8070
  shutdown: graceful
  servlet:
    context-path: /gateway

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui/index.html

spring:
  cloud:
    compatibility-verifier:
      enabled: false
  application:
    name: gateway
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/admin-exchange

keycloak:
  realm: admin-exchange
  auth-server-url: http://localhost:8080
  client-id: exchange-management
  client-secret: y8nXsnczJs97MnPQXQQpqdMa6C6kgqWw

#discovery:
#  url:
#    profile: localhost:8088/profile